# Local API & Backend
fastapi
uvicorn
pydantic
chromadb
python-multipart

# Data Science & RAPIDS (Install via Conda if possible for GPU support)
pandas
numpy
matplotlib
seaborn
scikit-learn

# NLP & NVIDIA Ecosystem
transformers
torch
openai  # For NIMs (often OpenAI-compatible API)

# On-device vision (face enrollment + "Who is this?" recognition)
insightface           # RetinaFace detector + ArcFace embeddings (buffalo_l)
onnxruntime           # Runs the InsightFace ONNX models locally
opencv-python-headless # Image decoding for the vision pipeline

# On-device speech (the voice companion loop)
faster-whisper        # Local STT (patient speech -> text)
piper-tts             # Local TTS (companion text -> warm voice)

# LLM Frameworks (RAG)
langchain
langchain-nvidia-ai-endpoints
llama-index

# Utils
requests
python-dotenv
tqdm
jupyterlab

# Testing
pytest                 # Backend test runner
httpx                  # FastAPI TestClient HTTP backend
